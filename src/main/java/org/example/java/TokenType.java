package org.example.java;

public enum TokenType {
    KEYWORD,
    IDENTIFIER,
    OPERATOR,
    INTEGER_LITERAL,
    CHAR_LITERAL,
    STRING_LITERAL,
    LPAREN, RPAREN, LBRACKET, RBRACKET,
    BEGIN, END, WRITE
}